# 16 — Decision Log

**Package:** FEMA Program ID & PRA Automation (demo)
**Document date:** 2026-07-08
**Status:** Conceptual demo. Records design decisions already made in this package, with the assumptions/requirements they rest on and the SME confirmation each still needs.
**Cross-references:** `REQ-` (02), `ASSUMP-` (03), `SRC-` (04), `SME-` (13).

---

## 1. Decision register

| Dec-ID | Decision | Rationale | Alternatives considered | Impact | Validation needed | Status |
|---|---|---|---|---|---|---|
| DEC-01 | **Synthetic disbursement ledger calibrated to public obligations** | Real spend is non-public; public data is obligations only; must not fabricate FEMA internals | Use public obligations as-is (wrong measure); invent "real-looking" spend (prohibited) | Core demo dataset; funding-vs-spend narrative | `SME-11` (spend definition, no-year handling) | ✅ Decided |
| DEC-02 | **Config-driven rules ("rules-as-data")** for mapping, rollup, event split, and trigger | Real rules undocumented; must be swappable when SOP arrives; must survive migration | Hard-code inferred rules (brittle); wait for SOP (blocks demo) | Whole engine architecture (files 06, 09) | `SME-02`, `SME-04` | ✅ Decided |
| DEC-03 | **Illustrative 10-question PRA**, labeled placeholder | Real instrument unavailable; must render a questionnaire without fabricating FEMA's form | Present a guessed "real" form (prohibited); omit the PRA (loses the deliverable) | Screen 7; file 10 | `SME-05` | ✅ Decided |
| DEC-04 | **Modern scripted/service implementation over a literal Excel macro** | "Macro" was a hedged recollection of the legacy form factor, not a requirement; client is "gung ho about automation/AI" | Build an actual Excel macro (limits AI, portability) | Tech stack (file 07); exports still Excel-compatible | `SME-03` | ✅ Decided (`ASSUMP-15`) |
| DEC-05 | **Deterministic core, AI on the edges** | Auditable numbers must not depend on model discretion | LLM computes/decides figures (unauditable, hallucination risk) | Files 06 §2, 09 §1 | `SME-15` | ✅ Decided |
| DEC-06 | **Mandatory human-in-the-loop sign-off** before any PRA finalizes | Improper-payment outputs are consequential; counters over-automation | Full auto-finalization (reputational + audit risk) | Screen 8; file 09 §10 | `SME-16` | ✅ Decided (`ASSUMP-17`) |
| DEC-07 | **Confidence threshold routes below-bar outputs to an exception queue** (default 0.85) | Mirrors today's manual adjustments; focuses review | Auto-accept all AI output; manual-review everything | Routing logic (files 06, 09) | `SME-15` | ✅ Decided (`ASSUMP-16`) |
| DEC-08 | **Configurable, on-screen 20% YoY trigger** (default: either direction, disbursements) | Threshold/direction/measure all hedged in transcript | Hard-code 20% increase-only (likely wrong) | Screen 6; file 10 §5 | `SME-01` | ✅ Decided (`ASSUMP-03`) |
| DEC-09 | **Real public identifiers as skeleton** (DR numbers `SRC-02`, assistance listings `SRC-03/12`) | Grounds synthetic data in verifiable anchors; recognizable to audience | Fully invented events/programs (less credible) | Data model (file 08 §4–5) | `SME-06` | ✅ Decided (`ASSUMP-08`, `ASSUMP-09`) |
| DEC-10 | **Watermark every synthetic record** (`SYNTHETIC-DEMO`) + on-screen captions | Prevent audience mistaking demo data for real | Rely on verbal disclaimer only (risk of misread) | All data screens | — | ✅ Decided (`ASSUMP-10`) |
| DEC-11 | **Two tech-stack options; build the demo on Option A (Streamlit/DuckDB/Python)** | Fixed demo date; portability; single language | Build enterprise stack now (too slow for demo) | File 07 | `SME-09` (for Option B pilot) | ✅ Decided |
| DEC-12 | **Cloud-portable, dependency-light build; no cloud in critical path** | Cloud stack unconfirmed (Azure vs AWS); demo date fixed | Commit to one cloud now (premature) | Files 06 §3, 07 §5 | `SME-09` | ✅ Decided (`ASSUMP-11`) |
| DEC-13 | **RAG corpus = public guidance only for the demo** (`SRC-06/07/10`) | Internal SOP unavailable/unreleasable; no fabrication | Load a guessed SOP (prohibited); skip RAG (loses grounding) | File 09 §8 | `SME-17` | ✅ Decided (`ASSUMP-18`) |
| DEC-14 | **File-in/file-out, source-agnostic contract** | Must survive pending financial-system migration | Bind to current system (breaks on migration) | Files 06 A2, 12 | `SME-10` | ✅ Decided (`ASSUMP-12`) |
| DEC-15 | **FedRAMP/ATO/security treated as production-only** (Wave 8), not demo constraints | Demo uses synthetic data, no PII; controls would block the concept | Attempt federal controls now (out of scope, slow) | Files 06 §6, 15 | `SME-09`, `SME-18` | ✅ Decided |
| DEC-16 | **~8/10 auto-populate split is parameterizable**, not fixed | Transcript hedged "probably like eight" | Hard-code 8/2 | File 10 §2 | `SME-05` | ✅ Decided |
| DEC-17 | **"Assessment lifecycle" terminology, no appeals workflow** | "Appeal life cycle" is a transcription artifact | Build an appeals process (nonexistent) | All narrative files | `SME-13` | ✅ Decided (`ASSUMP-14`) |
| DEC-18 | **Illustrative role model** (analyst / reviewer / admin) | Real RBAC unknown; demo needs a plausible sign-off model | No roles (weak governance story) | Files 06 §6, 07 | `SME-16` | ✅ Decided (`ASSUMP-19`) |
| DEC-19 | **Fixed generator seed `20260708`**; generator builds twice and byte-compares before writing; audit timestamps are a fixed constant | Reproducibility/testability requirement — re-runs must be byte-identical; wall-clock time would break that | Unseeded RNG (irreproducible); seed per-run (diff noise in git) | `data/generator/`, `data/synthetic/MANIFEST.sha256` | — | ✅ Decided (Wave 1) |
| DEC-20 | **Calibration basis = live per-DR obligation envelopes** (sum of `federalShareObligated` from `SRC-03` PA v2 + `SRC-04` HMA v4, pulled 2026-07-08, cached in `anchors.json`); cumulative synthetic disbursements per DR capped at ≤90% of envelope (actual max 29.5%) | Grounds synthetic disbursement magnitudes in verified public data without presenting obligations as spend (`ASSUMP-05/10`); cache removes network from re-runs | Static ranges from file 04 only (staler); no calibration (implausible magnitudes) | `data/generator/anchors.json`, `DATA_DICTIONARY.md` §2 | `SME-11` | ✅ Decided (Wave 1) |
| DEC-21 | **CSV as the primary committed data format** (UTF-8, `\n`, DuckDB-native); Parquet not committed, produced ad hoc | Text is git-diffable and human-inspectable at demo scale (~2k rows); DuckDB loads it natively | Parquet primary (binary, not diffable); both committed (drift risk) | `data/synthetic/`, `data/README.md` | — | ✅ Decided (Wave 1) |
| DEC-22 | **Ground-truth answer key emitted as separate validation-only files** (`answer_key.csv`/`.md`), excluded from the inference path; generator self-check scans the repo for references and fails on any | Wave 4 crawler (`REQ-013`) must rediscover mappings independently; the key exists only to score that inference later | No key (can't score inference); truth embedded in demo tables only (unscorable exceptions) | `data/synthetic/answer_key.*` | — | ✅ Decided (Wave 1) |
| DEC-23 | **Dataset fields added beyond file 08, flagged not silent**: `transaction.raw_code` (as-landed dirty code), `fiscal_year_spend_summary` table (program×FY PRA bindings, Q4–Q7), `data_watermark` on every table, `mapping_rule.rule_type='cleansing'` | The cleansing demo (`REQ-002/003`), file 10 §4 PRA bindings, and the watermark-every-record constraint need them; file 08 §6 tables are still emitted unchanged | Silently extend 08 (breaks traceability); omit (demo scenarios lose their data) | `DATA_DICTIONARY.md` §5; proposed for file 08's next revision | — | ✅ Decided (Wave 1) |

| DEC-24 | **Leave-behind demo = ONE self-contained HTML file** (`leavebehind/fema-demo.html`): all HTML/CSS/JS inline, Wave 1 dataset embedded as JSON, hand-rolled SVG charts | Must be emailable and double-click-runnable on a locked-down machine with no server, install or build step | Multi-file bundle (breaks on email/copy); hosted demo (needs network/access); heavy inlined chart framework (size, offline risk) | `leavebehind/` deliverables; complements (does not replace) the Wave 6 Streamlit build | `SME-09` (if it must run in a client tenant) | ✅ Decided (leave-behind pass) |
| DEC-25 | **No runtime network, no CDN, no browser storage** in the leave-behind: file renders identically offline; state is in-memory (reload = reset); every URL present is inert citation text | Locked-down environments block CDNs; storage APIs risk stale demo state and policy flags; offline behavior is itself a talking point | CDN-loaded libs/fonts (breaks offline); localStorage persistence (stale state across demos) | Build script statically rejects `fetch`/XHR/CDN/`localStorage` patterns | — | ✅ Decided (leave-behind pass) |
| DEC-26 | **AI outputs in the leave-behind are precomputed/deterministic template text**, labeled "AI-suggested (precomputed for demo)" and human-overridable; no API keys or live LLM calls in the file | A distributable offline file cannot hold secrets or reach a model; guardrail G1 already forbids AI computing numbers, so the explanatory layer can be templated without changing the concept | Embed an API key (prohibited); ship no AI texture (loses the story); fake a "live" model (dishonest) | Screens 3/4/6/7/8 rationale blocks; README disclosure | `SME-15` | ✅ Decided (leave-behind pass) |
| DEC-27 | **Deterministic core recomputed in embedded JavaScript**: rollups, event splits, YoY, trigger flags and PRA quant binds are calculated live in-browser from the embedded 2,019-row ledger; generator summaries are embedded only to power an in-app parity check (~740 values) | Proves rules-as-data and checkable math to the audience (a slider/rule edit visibly re-runs the real computation, not a lookup); parity check demonstrates the JS engine matches the committed generator output | Display precomputed CSVs only (no live re-flag wow moment); recompute without any parity proof (unverifiable) | Screens 1–8; screen 9 parity panel | — | ✅ Decided (leave-behind pass) |
| DEC-28 | **`fema-demo.html` is generated, not hand-edited**: `build_demo_html.py` (stdlib-only) inlines current CSVs + `rules.yaml` into `template.html`, byte-stable, verifying the watermark on every embedded row and refusing to read or reference the answer key (extends DEC-22) | Keeps the leave-behind reproducible as Wave 1 data evolves; keeps watermark/answer-key guarantees mechanical rather than by convention | Hand-maintained HTML (drifts from data); embedding at authoring time only (stale on regeneration) | `leavebehind/build_demo_html.py`, `template.html` | — | ✅ Decided (leave-behind pass) |
| DEC-29 | **Mapping-visualization features (enhancements shortlist FLOW-01/02/04/07) built into the leave-behind as deterministic engine re-use**: follow-the-dollar lineage overlay, dollar-weighted flow-map tab (hand-rolled fixed-4-column SVG, no layout library), top-bar code search resolving dirty/legacy spellings through the embedded cleansing rules, and a what-changed drawer (before/after snapshot diff with exact Revert) on every rule edit / exception approval | Makes the invisible ID→program linking itself visible (REQ-001/002/003/004/005) using only already-embedded Wave 1 data; zero new generator fields; no answer-key involvement; preserves DEC-25/26/27 guarantees (offline, labeled precomputed AI, deterministic numbers) | Build the deferred FLOW-05 crawler reveal now (overclaim risk on deliberately stable synthetic data); d3/d3-sankey via CDN (breaks DEC-25); bolt-on standalone pages (fragments the storyboard arc) | `leavebehind/template.html` (regenerated `fema-demo.html`); spec in `enhancements/MAPPING_DEMO_IDEAS.md` §4 | — | ✅ Decided (enhancement pass, 2026-07-09) |
| DEC-30 | **FLOW-05 crawler holdout reveal built into the leave-behind (screen 3 "Inference test" tab) with a fiscal-year holdout and code-isolated scoring**: the split constant `F5_HOLDOUT_FY = 2026` holds out FY2026 (train = every earlier FY, 396 mapping rows; changing the constant re-splits reproducibly); the inference function (`f5Infer`) receives only the training slice plus the unlabeled holdout-year intake transactions and mines cross-year grouping stability with **evidence-weighted confidence** (agreement × observation depth × raw-identity stability, capped 0.97) — so confidence varies per code (0.85–0.97 auto) and drops below the 0.85 bar exactly where the raw extract churned (3 retired legacy aliases + 1 sparse dirty code, caught by review), while never-seen codes get a low-confidence structural-similarity guess (event mix, fund, segment nearness, amount profile; 0.21–0.38); the scoring function (`f5Score`) is a separate path and the sole reader of the held-out FY2026 mapping rows (planted truth already embedded as ordinary data); the accuracy headline is computed at reveal time; runs are logged to the in-session audit trail | Delivers the transcript's own "crawler … 99% of the time these groupings are all together" claim (REQ-013/REQ-014) with verifiable holdout integrity; overclaim risk from DEC-29 is mitigated by the presenter honesty beat in `DEMO_SCRIPT.md` ("deliberately stable history ⇒ perfect score expected; real-world accuracy is SME-04/SME-07 validation") rather than on-screen disclosure (removed in the UX scrub); the `answer_key.*` files stay unread and un-embedded (DEC-22) — FY2026 rows already in the file are the runtime truth | Random per-code holdout (weaker story than "a year it never saw"; the FY split is also what the spec §2 FLOW-05 documents); reading the answer-key CSV at runtime (violates DEC-22/DEC-28 build assertion); planting a cross-year grouping change in the generator for a sub-100% score (changes every committed number and frozen doc; deferred as the spec's optional must-add) | `leavebehind/template.html` (regenerated `fema-demo.html`); spec in `enhancements/MAPPING_DEMO_IDEAS.md` §2 FLOW-05 | `SME-04`, `SME-07` | ✅ Decided (FLOW-05 pass, 2026-07-09) |

---

## 2. Decisions explicitly deferred (not yet made)

| Open decision | Blocked on | Where it lands |
|---|---|---|
| Exact trigger measure/threshold/direction | `SME-01` | File 10 config |
| Real input schema / macro reality | `SME-03` | File 08, ingestion |
| Real PRA question text & bindings | `SME-05` | File 10 |
| Target cloud & tenant | `SME-09` | File 07 Option B |
| Production security/RBAC/retention | `SME-16/17/18` | File 12 Wave 8 |
| Reporting output format | `SME-14` | Screen 10 |

---

## 3. New IDs coined across the design pass (consolidated)

| ID | Meaning | First defined in |
|---|---|---|
| `ASSUMP-16` | Below-confidence outputs route to exception queue (default 0.85) | file 05 |
| `ASSUMP-17` | Mandatory human sign-off before PRA finalizes | file 05 |
| `ASSUMP-18` | RAG corpus = public guidance only for the demo | file 06 |
| `ASSUMP-19` | Illustrative role model (analyst/reviewer/admin) | file 06 |
| `SME-14` | Reporting outputs/formats (resolves REQ-006 "etc.") | file 05 |
| `SME-15` | Explainability/evidence + confidence thresholds | file 05 |
| `SME-16` | Users/roles/RBAC + sign-off authority | file 05 |
| `SME-17` | RAG corpus availability/releasability | file 06 |
| `SME-18` | Audit-trail/retention/immutability | file 05 |

> `REQ-`, `SRC-` prefixes were **not** extended in the design pass — no new requirements or public sources were coined; all citations resolve to the frozen foundation IDs.
