# Wave 1 reproducibility: the synthetic dataset and its generator are written
# with LF endings and verified byte-identical against MANIFEST.sha256.
# Prevent autocrlf from rewriting them on checkout.
solution-design/fema-program-id-risk-assessment/data/** text eol=lf

# Leave-behind reproducibility: build_demo_html.py must regenerate
# fema-demo.html byte-identically from a fresh clone, so the template,
# builder and generated file keep LF endings on every platform.
solution-design/fema-program-id-risk-assessment/leavebehind/** text eol=lf

# Binary assets must never be EOL-normalized — doing so strips CR bytes from
# the compressed ZIP data and corrupts the file (Word reports it as damaged).
# Last match wins, so these override the broad "leavebehind/** text eol=lf"
# rule above, which was mistakenly treating the .docx as text.
*.docx binary
*.xlsx binary
*.pptx binary
*.pdf  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.zip  binary

# Pilot engine reproducibility: the engine, its tests and its packaging travel
# to the FEMA-side machine as text in payload packets, hashed per file. CRLF
# rewriting on checkout would break every SHA-256 comparison at that boundary.
src/** text eol=lf
tests/** text eol=lf
pyproject.toml text eol=lf
